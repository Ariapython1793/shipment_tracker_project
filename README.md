# Container Tracking System 🚢

A Django-based web application for managing and tracking shipping containers.  
It provides a dashboard, CRUD operations, search filters, and CSV export.

---

## ✨ Features
- **Dashboard**: Overview of container statuses (Arrived, In Transit, Delayed).
- **Container Management**: Create, update, delete containers with details like origin, destination, distance, speed.
- **Search & Filter**: Search by container number, origin, destination, or status.
- **Delay Detection**: Automatic calculation of ETA and delay status.
- **CSV Export**: Export container data to CSV for reporting.
- **Admin Panel**: Manage containers via Django admin.

---

## 🛠️ Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/container_tracking.git
   cd container_tracking
